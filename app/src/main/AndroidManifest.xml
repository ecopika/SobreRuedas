<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ub.pis2016.dperezgu12alumnes.sobreruedas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".joc.Vista.activities.MenuActivity">

        </activity>
        <activity android:name=".joc.Vista.activities.seleccioPersonatgeActivity" />
        <activity android:name=".joc.Vista.activities.OpcionsActivity" />
        <activity android:name=".joc.Vista.activities.PerfilActivity" />
        <activity android:name=".joc.Vista.activities.TaulaPuntuacioActivity" />
        <activity android:name=".joc.Vista.activities.CreditsActivity" />
        <activity
            android:name=".joc.Vista.activities.IntroActivity"
            android:label="@string/title_activity_intro"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>
